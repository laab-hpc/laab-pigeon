[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pigeon-client"
version = "0.1.0"
description = "Pigeon enables data exchange between client applications and a disptach service via secured intermediate server. This package provides the client component of pigeon"
readme = { file = "README.md", content-type = "text/markdown" }
authors = [
    { name = "Aravind Sankaran", email = "a.sankaran@fz-juelich.de" }
]

requires-python = ">3"

classifiers = [
    "Programming Language :: Python :: 3"
]

dependencies = [
    "requests>=2.31",
]

[project.optional-dependencies]
dev = [
  "pytest>=7",
]

[project.scripts]
pigeon-client = "pigeon_client.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
include = ["pigeon_client*"]
